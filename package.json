{
  "name": "nuri-p2p",
  "version": "0.1.0",
  "description": "Peer-to-peer token swaps on Base. No contract of ours, no server, no operator.",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test 'test/**/*.test.mjs'",
    "check": "node --check src/offer.mjs && node --check src/board.mjs && node --check src/fill.mjs && node --check src/constants.mjs",
    "test:page": "node scripts/build-smoke.mjs && node scripts/page-smoke.mjs",
    "test:live": "node scripts/build-smoke.mjs && node scripts/page-live.mjs",
    "prove:board": "node scripts/prove-board.mjs",
    "prove": "node scripts/prove.mjs",
    "prove:stale": "node scripts/prove-stale.mjs"
  },
  "dependencies": {
    "ethers": "^6.13.4",
    "nostr-tools": "^2.10.4"
  },
  "license": "MIT"
}
